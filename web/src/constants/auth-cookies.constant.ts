/** Must match api/src/constants/auth-cookies.constant.ts - the two apps don't share a package,
 * so these names are duplicated here on purpose rather than left implicit. */
export const REFRESH_COOKIE_NAME = 'refresh_token';
export const STAFF_REFRESH_COOKIE_NAME = 'staff_refresh_token';
