{
  "name": "school-management-system",
  "private": true,
  "scripts": {
    "check": "bash check.sh",
    "postinstall": "npm --prefix web install && npm --prefix api install",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
